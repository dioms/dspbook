<div class="hero-unit">
  <h1>  <%= image_tag @user.profpic.url(:thumb) unless !@user.profpic.exists? %>  <%= @user.name.capitalize %>
    <% if user_signed_in? %>
      <% if current_user.id == @user.id %> 
        <%= link_to 'Edit Profile', edit_user_registration_path, :class => "btn btn-primary" %></h1>
    <% else %>
    </h1>
  <% end %>
<% else %>
</h1>
<% end %>
<%= link_to 'Resume', @user.resume.url unless !@user.resume.exists? %> <br />
Email: <%= @user.email %> <br />
<div class="well">
  <%= "Bio: " + @user.bio  unless @user.bio.blank? %>
</div>
<div>
